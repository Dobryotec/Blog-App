export type Data = {
  createdAt: string;
  title: string;
  content: string;
  image: string;
  id: string;
};

export type Comments = {
  createdAt: string;
  name: string;
  avatar: string;
  comment: string;
  id: string;
};
